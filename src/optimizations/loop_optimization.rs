/*
use crate::{
    base_blocks::BlockGraph,
    optimizations::{
        constant_propagation::ConstantPropagation, live_variables::LiveVariables, tarjan::Scc,
    },
};

impl BlockGraph {
    pub fn loop_optimization(
        &mut self,
        live_variables: &LiveVariables,
        constant: &ConstantPropagation,
    ) {
    }
}
*/
